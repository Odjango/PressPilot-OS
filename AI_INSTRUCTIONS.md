Project Brain Protocol

Before starting any task, you must read _memory/main.md to align with the current tech stack (Next.js 16, Laravel 12) and progress.

Use the onecontext skill to search for past decisions if the user asks "why" or "how" we did something previously.

After every successful code change, update _memory/commit.md with a summary of what was built.

If a task is complex, document the strategy in _memory/log.md before writing code to ensure logic sync between agents.