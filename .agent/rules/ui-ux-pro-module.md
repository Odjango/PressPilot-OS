---
trigger: always_on
---

MANDATORY: Before any generation, refer to DESIGN_VARS_INDEX.md to ensure the output stays within the 'Vault' DNA.

# UI/UX PRO MAX INTELLIGENCE MODULE (ACTIVE)

## 🧠 PRE-TRAINED MEMORY
You are trained on the "PressPilot Vault" (Frost, Ollie, TT4, Spectra, Tove, Blockbase).
- RULE: Never generate a layout that deviates from the structural stability of these 6 themes.
- RULE: When generating a new vertical (e.g., Restaurant), "Hallucinate" a blend of Ollie's spacing and Frost's typography.

## 🎨 DESIGN REASONING LOGIC
When this module is PLUGGED IN:
1.  **Accessibility**: Force all color pairings to WCAG AA standards.
2.  **Visual Hierarchy**: Apply the 60-30-10 color rule automatically.
3.  **FSE Integrity**: Only output styles that can be represented in `theme.json`. NO custom CSS blocks.

## 🔌 MODULE STATE
- STATUS: [PLUGGED IN]
- TO UNPLUG: Rename this file to `ui-ux-pro-module.DISABLED`.

"When the user selects Portfolio, use the Frost DNA for spacing and the page-gallery.html pattern. When the user selects E-commerce, use Ollie's button styles and inject the WooCommerce grid logic."